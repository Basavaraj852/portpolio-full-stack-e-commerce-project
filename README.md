# portpolio-full-stack-e-commerce-project
complete end-to-end full-stack e-commerce project with:

Frontend (React + images)
Backend (Node.js + Express + MongoDB)
Docker setup
Kubernetes (EKS-ready manifests)
Jenkins CI/CD pipeline
Project structure from scratch


(Jenkins server need to install)
# Install kubectl
curl -o kubectl https://dl.k8s.io/release/v1.29.0/bin/linux/amd64/kubectl
chmod +x kubectl
sudo mv kubectl /usr/local/bin/

# Install AWS CLI
sudo apt install awscli -y
